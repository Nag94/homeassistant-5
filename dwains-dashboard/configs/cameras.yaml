# https://dwainscheeren.github.io/dwains-lovelace-dashboard/configuration/cameras.html

cameras:
  main_menu: 'true' #If you want to hide the cameras in the main nav then enable this line
  live_view: 'true' #If you want to disable live view then enable this line
  entities:
    - camera.nest_cam
    - camera.nest_iq_cam